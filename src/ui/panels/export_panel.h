// Export dialog. Lets the user pick a preset, choose a destination
// path, kick off the encode, and watch progress.
#pragma once

#include "ui/main_layout.h"

namespace volchay::ui::panels {

void draw_export(EditorContext& ctx);

}  // namespace volchay::ui::panels
